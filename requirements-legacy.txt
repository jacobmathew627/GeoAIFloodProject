# Optional: only needed to run src/inference_final.py, the archived PyTorch
# U-Net path. The supported pipeline (susceptibility.py + hazard.py) does not
# use PyTorch.
-r requirements.txt

torch==2.3.0

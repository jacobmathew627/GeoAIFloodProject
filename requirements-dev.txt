# Development dependencies for GeoAI Flood Risk Project
# Install with: pip install -r requirements-dev.txt

# Core (already in requirements.txt)
-r requirements.txt

# Testing
pytest==8.2.0
pytest-cov==5.0.0
pytest-mock==3.14.0
pytest-asyncio==0.23.0
# fastapi.testclient (tests/test_backend_api.py) is a thin wrapper over
# httpx, which FastAPI does not pull in as a runtime dependency. It was
# present locally as a transitive install, so the API tests passed here while
# CI -- which builds from these files alone -- would have failed to collect
# them.
httpx==0.27.0

# Code quality
ruff==0.5.0
black==24.4.0
mypy==1.10.0
pre-commit==3.7.0

# Type stubs
types-requests==2.31.0
types-Pillow==10.0.0

# Development tools
ipython==8.24.0
jupyter==1.0.0
notebook==7.1.0

# Documentation
mkdocs==1.5.0
mkdocs-material==9.5.0
mkdocstrings[python]==0.25.0